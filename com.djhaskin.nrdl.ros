#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  #+quicklisp(ql:quickload '() :silent t)
  (asdf:load-system "com.djhaskin.nrdl")
  )

(defpackage :ros.script.nrdl.3892487071
  (:use :cl)
  (:import-from :com.djhaskin.nrdl generate-to))
(in-package :ros.script.nrdl.3892487071)

(defun main (&rest argv)
  (declare (ignorable argv))
  (generate-to t '(1 2 3) :pretty-indent 4))
;;; vim: set ft=lisp lisp:
